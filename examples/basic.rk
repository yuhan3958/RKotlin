class Point {
    var x: Int32;
    var y: Int32;

    fun sum(): Int32 {
        return x + y;
    }
}

fun main(): Int32 {
    val point: Point = new Point();
    point.x = 20;
    point.y = 22;

    println("point sum:");
    println(point.sum());
    return point.sum();
}
