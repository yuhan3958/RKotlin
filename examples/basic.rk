import rkotlin.Array;

fun main() {
    val array = arrayOf(1, 2, 3, 4, 5, 6, 100, 101)

    println(array.length())
    for (i in array) {
        println(i)
    }

    val a = array.iterator()
    val b = array.iterator()

    println(a.next())
    println(b.next())
    println(a.next())
}